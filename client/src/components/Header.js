import UserMenu from "./UserMenu";

const Header = () => {

    return (
        <header>
            <span id="logo">ToDo List</span>
            <UserMenu />
        </header>
    );

}

export default Header;